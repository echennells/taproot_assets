# Taproot Assets Extension Configuration
# =====================================
# Copy this file to taproot_assets.conf and update with your settings

# Configuration Modes
# ------------------
# This extension supports two connection modes:
#
# 1. INTEGRATED MODE (Lightning Terminal - LiT)
#    - Extension connects directly to integrated Taproot Assets daemon
#    - Requires this configuration file with specific settings
#    - Taproot Assets runs on same gRPC port as LND (usually 10009)
#    - Uses separate .tapd directory with its own macaroons
#
# 2. STANDALONE MODE
#    - Extension connects to separately running tapd instance
#    - tapd runs on its own port (usually 10029)
#    - Useful when running tapd as independent service

# When to use this config file:
# - ALWAYS when using Taproot Assets (both integrated and standalone)
# - LNbits can use any wallet backend (LndRestWallet, LndGrpcWallet, etc.)
# - Extension needs explicit connection details for Taproot Assets

# Connection Settings
# ------------------

# TAPD host and port
# For Lightning Terminal (integrated): localhost:10009 (same as LND gRPC)
# For standalone tapd: localhost:10029 (tapd's dedicated port)
# IMPORTANT: Check with 'ss -ntlp' to see what ports are actually listening
TAPD_HOST=127.0.0.1:10009

# Network: mainnet, testnet, regtest, simnet, signet
# Must match your Lightning node's network
TAPD_NETWORK=mainnet

# TLS Certificate Path
# For Lightning Terminal: Use LND's certificate (NOT LiT's certificate)
# For standalone tapd: Use tapd's certificate
TAPD_TLS_CERT_PATH=/home/user/.lnd/tls.cert

# Taproot Assets Macaroon Path
# Even with integrated Lightning Terminal, tapd creates its own .tapd directory
# with network-specific macaroons. Do NOT use LiT's macaroon.
TAPD_MACAROON_PATH=/home/user/.tapd/data/{network}/admin.macaroon

# Optional: Use hex-encoded macaroon instead of file path
# Useful for environments where file access is restricted
# TAPD_MACAROON_HEX=0201036c6e6402...

# LND Settings (for operations requiring LND access)
# -------------------------------------------------

# LND Macaroon Path (used for Lightning operations)
# Local example: /home/ubuntu/.lnd/data/chain/bitcoin/mainnet/admin.macaroon
# Docker example: /root/.lnd/data/chain/bitcoin/mainnet/admin.macaroon
LND_REST_MACAROON=/home/ubuntu/.lnd/data/chain/bitcoin/mainnet/admin.macaroon

# Optional: Use hex-encoded LND macaroon
# LND_MACAROON_HEX=0201036c6e6402...

# Fee Settings
# -----------

# Default fee in satoshis for on-chain transactions
TAPD_DEFAULT_SAT_FEE=1

# Docker Configuration Example
# ---------------------------
# If running in Docker, use these paths instead:
#
# TAPD_HOST=lit:10009
# TAPD_TLS_CERT_PATH=/root/.lnd/tls.cert
# TAPD_MACAROON_PATH=/root/.tapd/data/mainnet/admin.macaroon
# LND_REST_MACAROON=/root/.lnd/data/chain/bitcoin/mainnet/admin.macaroon

# Environment Variables
# --------------------
# All settings can also be provided via environment variables for containerized deployments.
# Environment variables take precedence over this config file.
# Use the same names as shown above (e.g., TAPD_HOST, TAPD_NETWORK, etc.).